syntax = "proto3";

package v1;

// TriggerPolicy enum
enum TriggerPolicy {
    all_succeeded = 0;
    all_failed = 1;
    all_done = 2;
    one_succeeded = 3;
    one_failed = 4;
    one_done = 5;
}
