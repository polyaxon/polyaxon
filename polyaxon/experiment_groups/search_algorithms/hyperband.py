import numpy as np

from experiment_groups.experiment_declaration import ExperimentDeclaration


def get_suggestions(matrix_params, n_experiments):
    """Return a list of suggestions based on hyperband search.

    Params:
        matrix_params: `dict` representing the {hyperparam: hyperparam matrix config}.
        n_suggestions: number of suggestions to make
    """
    pass
