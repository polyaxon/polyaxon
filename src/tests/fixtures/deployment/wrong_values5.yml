deploymentType: docker-compose

rbac:
  enabled: true

gateway:
  service:
    type: ClusterIP

environment: staging

user:
  password: root

postgresql:
  enabled: false  # Requires external service
