deploymentType: docker-compose

rbac:
  enabled: true

gateway:
  service:
    type: ClusterIP

environment: staging

user:
  password: root

scheduler:
  enabled: true

rabbitmq-ha:
  enabled: false  # Requires external service
