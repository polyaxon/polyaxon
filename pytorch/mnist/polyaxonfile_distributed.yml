---
version: 1

project:
  name: mnist

environment:
  pytorch:
    n_workers: 3

run:
  image: pytorch/pytorch:latest
  steps:
    - pip install --no-cache-dir -U polyaxon-helper
  cmd: python distributed_run.py
