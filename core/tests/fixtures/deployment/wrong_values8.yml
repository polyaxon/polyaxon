deploymentType: docker-compose

rbac:
  enabled: true

gateway:
  service:
    type: ClusterIP

environment: staging

user:
  password: root

postgresql:
  enabled: true

redis:
  enabled: true

rabbitmq-ha:
  enabled: true

scheduler:
  enabled: true
