deploymentType: docker-compose

rbac:
  enabled: true

gateway:
  service:
    type: ClusterIP

environment: staging

user:
  password: root

broker: redis

redis:
  enabled: false  # Requires external service

scheduler:
  enabled: true
