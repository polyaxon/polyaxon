import { settingsURL } from '../base';

export const ClusterMoreSettingsURL = `${settingsURL}more`;
export const ClusterIntegrationsSettingsURL = `${settingsURL}integrations/`;
export const ClusterReposSettingsURL = `${settingsURL}repos/`;
