---
title: "AWS CLI"
description: "Performs operations on Google Cloud Platform resources using gcloud."
ogImage: /images/integrations/aws.png
---

This is a simple component to perform operations on AWS resources using the AWS CLI.

## Component info on Polyaxon Hub

To learn about this component definition or customize its behavior, please check [aws-cli on polyaxon-hub](https://github.com/polyaxon/polyaxon-hub/tree/master/aws-cli).

## Run this component using the CLI

To run the latest version of this component using Polyaxon CLI:

```bash
polyaxon run --hub aws-cli -P ...
```

To use a specific version:


```bash
polyaxon run --hub aws-cli:[tag] -P ...
```

> **Note**: `-P` is for passing parameters, e.g. `-P param1=value1 -P parame2=value2`


## View a running operation on the dashboard

After running an operation with this component, you can view it on the dashboard:

```bash
polyaxon ops dashboard
```

or

```bash
polyaxon ops dashboard -p [project-name] -uid [run-uuid] -y
```


## Stop a running operation

To stop a running operation with this component:

```bash
polyaxon ops stop
```

or

```bash
polyaxon ops stop -p [project-name] -uid [run-uuid]
```

## Run this component using the client

To run this component using Polyaxon Client:

```python
from polyaxon.client import RunClient

client = RunClient(...)
client.create_from_hub(component="aws-cli", ...)
```

## Usage in operations

You can also create operations instead of passing params:

```yaml
version: 1.1
kind: operation
params:
  param1: {value: value1}
  ...
hubRef: aws-cli:[tag]
```

## Versions

This component has the following versions: `['latest', '2.0.21']`
