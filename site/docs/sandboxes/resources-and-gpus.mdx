---
title: "Resources and GPUs"
description: "Request CPU, memory, GPUs, queues, and node placement for sandbox-enabled runs."
label: "v1.16+"
---

# Resources and GPUs

Sandboxes consume cluster resources for as long as the backing service run is active. Request only what the session needs and stop it when work is complete.

This page reuses `sandbox/sandbox.yaml` and the `quick-start` project from the [Sandbox Quick Start](/docs/sandboxes/quick-start/). Each Python or CLI run example below starts a new service. Python returns the UUID on the run object; for later CLI commands, replace the current value of `RUN_UUID` with the new run UUID.

## Select a Queue

Use queues to route sandboxes to the right agent (compute cluster) or resource pool:

<Tabs items={["Python", "CLI"]}>

<Tab>

```python
from polyaxon.client import RunClient

queue_client = RunClient(project="quick-start")
queue_run = queue_client.create_from_polyaxonfile(
    polyaxonfile="sandbox/sandbox.yaml",
    queue="agent-name/queue-name",
    approved=True,
)
print(queue_run.uuid)
```

</Tab>

<Tab>

```bash
polyaxon run -p quick-start -f sandbox/sandbox.yaml -q agent-name/queue-name
```

</Tab>
</Tabs>

See [queues](/docs/scheduling/scheduling-strategies/queues/) for scheduling behavior.

## Request Resources

Set CPU, memory, and accelerator requests under `run.container.resources`. Presets and cluster policies can patch or constrain these values:

```yaml
run:
  kind: service
  container:
    image: python:3.11
    resources:
      requests:
        cpu: "1"
        memory: 2Gi
      limits:
        cpu: "2"
        memory: 4Gi
        nvidia.com/gpu: "1"
    command: ["sleep", "infinity"]
```

For reusable sandbox profiles, prefer presets:

<Tabs items={["Python", "CLI"]}>

<Tab>

```python
from polyaxon.client import RunClient

gpu_client = RunClient(project="quick-start")
gpu_run = gpu_client.create_from_polyaxonfile(
    polyaxonfile="sandbox/sandbox.yaml",
    presets=["gpu-dev"],
    approved=True,
)
print(gpu_run.uuid)
```

</Tab>

<Tab>

```bash
polyaxon run -p quick-start -f sandbox/sandbox.yaml --presets gpu-dev
```

</Tab>
</Tabs>

## Use GPUs

GPU sandboxes are useful for profiling, interactive model debugging, and notebook exploration. Make sure the selected queue can schedule GPU workloads and that the image matches the GPU runtime requirements.

For node selectors, priority, annotations, and other Kubernetes placement controls, see [resources scheduling](/docs/scheduling/scheduling-strategies/resources-scheduling/) and [node scheduling](/docs/scheduling/scheduling-strategies/node-scheduling/).

## Cost Controls

Use timeouts and culling presets for long-running interactive services:

- [Service timeouts](/docs/scheduling/scheduling-presets/services-timeout/)
- [Cost estimation](/docs/scheduling/scheduling-strategies/estimating-cost/)

See the Python [run client reference](/docs/references/python-library/run-client/) or the [run CLI reference](/docs/references/cli/run/) for the full interfaces.
