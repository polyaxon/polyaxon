---
version: 0.6

kind: job

tags:
  foo: bar

inputs:
  - name: loss
    type: str
    default: MeanSquaredError
    is_optional: true

params:
  num_masks: 2

container:
  image: my_image
  command: ["/bin/sh", "-c"]
  args:
    - video_prediction_train
    - --num_masks={{num_masks}}
    - --loss={{loss}}
...
