---
version: 1

kind: notebook

tags: ['foo', 'bar']

environment:
  data_persistence: ['data1', 'data2']
  outputs_persistence: 'outputs1'
  node_selectors:
    polyaxon.com: node_for_notebook_jobs

build:
  image: my_image
  build_steps:
    - pip install package1
  env_vars:
    - ['KEY1', 'en_US.UTF-8']
    - ['KEY2', 2]
...
