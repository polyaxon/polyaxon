---
version: 1

kind: build

environment:
  node_selectors:
    polyaxon.com: node_for_build_jobs

build:
  image: my_image
  build_steps:
    - pip install package1
  env_vars:
    - ['KEY1', 'en_US.UTF-8']
    - ['KEY2', 2]
...
